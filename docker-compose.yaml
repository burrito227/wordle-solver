services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: wordle-solver-db
    ports:
      - "1433:1433"
    environment:
      ACCEPT_EULA: ${ACCEPT_EULA:-Y}
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      MSSQL_PID: ${MSSQL_PID:-Developer}
    volumes:
      - sqlserver_data:/var/opt/mssql
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P ${MSSQL_SA_PASSWORD} -Q 'SELECT 1'"]
      interval: 30s
      timeout: 10s
      retries: 3

  wordle-solver:
    build: .
    container_name: wordle-solver
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
      - DJANGO_DEBUG=${DJANGO_DEBUG}
      - DJANGO_ALLOWED_HOSTS=${DJANGO_ALLOWED_HOSTS}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=sqlserver       # Service name, not variable
      - DB_PORT=${DB_PORT} 
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
    depends_on:
      - sqlserver
    volumes:
      - ./wordle_solver:/app                     # For development
    working_dir: /app
    #entrypoint: ["/bin/bash", "-c", "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"]
    restart: unless-stopped

volumes:
  sqlserver_data: